spring:
  datasource:
    url: jdbc:h2:mem:persiqa;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=TRUE
    username: sa
    password: ""
  flyway:
    locations: classpath:db/h2/migration
  jpa:
    hibernate:
      ddl-auto: validate
